import 'package:flutter/material.dart';

class YouTubePage extends StatelessWidget {
  const YouTubePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('YouTubePage');
  }
}
