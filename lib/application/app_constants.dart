class BoxKeys {
  BoxKeys._();
  static const settings = 'settings';
}

class PKeys {
  PKeys._();
  static const themeMode = 'themeMode';
  static const locale = 'locale';
}

const kAppLocale = {
  'en': 'English',
  'vi': 'Tiếng Việt',
};
