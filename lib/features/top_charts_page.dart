import 'package:flutter/material.dart';

class TopChartsPage extends StatelessWidget {
  const TopChartsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('TopChartsPage');
  }
}
